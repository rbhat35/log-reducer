#include <stdlib.h>
#include <stdio.h>
#include <intel-pt.h>








int main() {
    printf("Starting pt.\n");
	struct pt_config config;
	pt_config_init(&config);
	//ptdump_tracking_init(&tracking);

}
